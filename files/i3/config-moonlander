# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $sup Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
font xft:URWGothic-Book 11

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+semicolon exec i3-sensible-terminal

# kill focused window
bindsym $mod+k kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
bindsym $mod+e exec --no-startup-id ulauncher

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+t focus left
bindsym $mod+s focus down
bindsym $mod+r focus up
bindsym $mod+n focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+$sup+t move left
bindsym $mod+$sup+s move down
bindsym $mod+$sup+r move up
bindsym $mod+$sup+n move right

# alternatively, you can use the cursor keys:
bindsym $mod+$sup+Left move left
bindsym $mod+$sup+Down move down
bindsym $mod+$sup+Up move up
bindsym $mod+$sup+Right move right

# split in horizontal orientation
bindsym $mod+q split h

# split in vertical orientation
bindsym $mod+comma split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+BackSpace layout stacking
bindsym $mod+a layout tabbed
bindsym $mod+u layout toggle split

# toggle tiling / floating
bindsym $mod+g floating toggle

# change focus between tiling / floating windows
bindsym $mod+h focus mode_toggle

# focus the parent container
#bindsym $mod+q focus parent

# focus the child container
#bindsym $mod+d focus child

workspace 1 output DP-0
workspace 5 output DP-2
workspace 12 output DP-4

set $KP_1 87
set $KP_2 88
set $KP_3 89
set $KP_4 83
set $KP_5 84
set $KP_6 85
set $KP_7 79
set $KP_8 80
set $KP_9 81
set $KP_0 90
# switch to workspace
bindcode $mod+mod2+$KP_1 workspace 1
bindcode $mod+mod2+$KP_2 workspace 2
bindcode $mod+mod2+$KP_3 workspace 3
bindcode $mod+mod2+$KP_4 workspace 4
bindcode $mod+mod2+$KP_5 workspace 5
bindcode $mod+mod2+$KP_6 workspace 6
bindcode $mod+mod2+$KP_7 workspace 7
bindcode $mod+mod2+$KP_8 workspace 8
bindcode $mod+mod2+$KP_9 workspace 9
bindcode $mod+mod2+$KP_0 workspace 10
bindsym $mod+b workspace B
bindsym $mod+eacute workspace É
bindsym $mod+p workspace P
bindsym $mod+o workspace O
bindsym $mod+v workspace V
bindsym $mod+d workspace D
bindsym $mod+l workspace L
bindsym $mod+j workspace J
bindsym $mod+z workspace Z
bindsym $mod+w workspace W

# move focused container to workspace
bindcode $mod+$sup+mod2+$KP_1 move container to workspace 1
bindcode $mod+$sup+mod2+$KP_2 move container to workspace 2
bindcode $mod+$sup+mod2+$KP_3 move container to workspace 3
bindcode $mod+$sup+mod2+$KP_4 move container to workspace 4
bindcode $mod+$sup+mod2+$KP_5 move container to workspace 5
bindcode $mod+$sup+mod2+$KP_6 move container to workspace 6
bindcode $mod+$sup+mod2+$KP_7 move container to workspace 7
bindcode $mod+$sup+mod2+$KP_8 move container to workspace 8
bindcode $mod+$sup+mod2+$KP_9 move container to workspace 9
bindcode $mod+$sup+mod2+$KP_0 move container to workspace 10
bindsym $mod+$sup+b move container to workspace B
bindsym $mod+$sup+eacute move container to workspace É
bindsym $mod+$sup+p move container to workspace P
bindsym $mod+$sup+o move container to workspace O
bindsym $mod+$sup+v move container to workspace V
bindsym $mod+$sup+d move container to workspace D
bindsym $mod+$sup+l move container to workspace L
bindsym $mod+$sup+j move container to workspace J
bindsym $mod+$sup+z move container to workspace Z
bindsym $mod+$sup+w move container to workspace W



set $config_i3 I3 : reload (R), restart (S)
mode "$config_i3" {
    bindsym r reload
    bindsym s restart

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+i mode "$config_i3"
# reload the configuration file
# bindsym $mod+$sup+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
# bindsym $mod+$sup+q restart
# exit i3 (logs you out of your X session)

# resize window (you can also use the mouse for that)
mode "resize" {
    # These bindings trigger as soon as you enter the resize mode

    # Pressing left will shrink the window’s width.
    # Pressing right will grow the window’s width.
    # Pressing up will shrink the window’s height.
    # Pressing down will grow the window’s height.
    bindsym t resize shrink width 10 px or 10 ppt
    bindsym s resize grow height 10 px or 10 ppt
    bindsym r resize shrink height 10 px or 10 ppt
    bindsym n resize grow width 10 px or 10 ppt

    # same bindings, but for the arrow keys
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+c mode resize

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Alt+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id i3exit lock, mode "default"
    bindsym e exec --no-startup-id i3exit logout, mode "default"
    bindsym s exec --no-startup-id i3exit suspend, mode "default"
    bindsym h exec --no-startup-id i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id i3exit reboot, mode "default"
    bindsym $sup+s exec --no-startup-id i3exit shutdown, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Delete mode "$mode_system"

bindsym $mod+x exec --no-startup-id nautilus --no-desktop
bindsym $mod+y exec google-chrome-stable
#bindsym $mod+F2 exec xrandr --output eDP-1 --right-of HDMI-1
#bindsym $mod+F3 exec xrandr --output eDP-1 --left-of HDMI-1

# Manage audio
bindsym XF86AudioRaiseVolume exec "amixer -q sset Master,0 5%+ unmute"
bindsym XF86AudioLowerVolume exec "amixer -q sset Master,0 5%- unmute"
bindsym XF86AudioMute exec "amixer -q sset Master,0 toggle"

bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioPause exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# Tools
bindsym XF86Calculator exec "gnome-calculator"
bindsym Print exec "gnome-screenshot -i"

# Screen brightness controls
bindsym XF86MonBrightnessUp exec brightnessctl -c backlight set +10% # increase screen brightness
bindsym XF86MonBrightnessDown exec brightnessctl -c backlight set 10%- # decrease screen brightness

# Keyboard tools
bindcode 132 exec "keyboard i3 moonlander"
bindcode $mod+132 exec "keyboard i3 toggle"
bindsym $mod+twosuperior exec "keyboard i3 default"
bindsym Hiragana_Katakana exec "keyboard i3 ymdk40"
bindsym XF86Tools exec "keyboard kbmap quit"
bindsym XF86Launch5 exec "keyboard kbmap 1"
bindsym XF86Launch6 exec "keyboard kbmap 2"
bindsym XF86Launch7 exec "keyboard kbmap 3"
bindsym XF86Launch8 exec "keyboard kbmap 4"
bindsym XF86Launch9 exec "keyboard kbmap 5"

# Floating windows
for_window [instance="gnome-calculator"] floating enable
for_window [instance="ulauncher"] floating enable
for_window [instance="Blender" title="Blender Preferences"] floating enable
for_window [instance="pulseUI"] floating enable
for_window [class="feh-kbmap"] floating enable
for_window [all] title_window_icon on
for_window [all] title_window_icon padding 10px

exec_always --no-startup-id /home/jngermon/.screenlayout/home.sh
exec_always --no-startup-id /home/jngermon/.dotfiles/files/polybar/launch.sh
exec_always --no-startup-id /home/jngermon/.dotfiles/files/i3/wallpaper.sh
exec_always --no-startup-id numlockx
exec_always --no-startup-id nm-applet
exec_always --no-startup-id /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1 &
exec_always --no-startup-id picom

# Theme colors
# class                   border  backgr. text    indic.   child_border
  client.focused          #556064 #556064 #E3A914 #FD6517
  client.focused_inactive #2F3D44 #2F3D44 #fba922 #454948
  client.unfocused        #2F3D44 #2F3D44 #fba922 #454948
  client.urgent           #CB4B16 #FD6517 #fba922 #268BD2
  client.placeholder      #000000 #0c0c0c #ffffff #000000
  client.background       #2B2C2B

